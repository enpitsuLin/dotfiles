# ~/.bashrc — minimal bash compatibility layer.
# zsh is the primary shell; bash is only used by scripts/CI/integrated terminals.
# Keep the two tool paths that matter most for non-interactive bash use.
export PATH="$HOME/.local/bin:$HOME/.cargo/bin:$PATH"
